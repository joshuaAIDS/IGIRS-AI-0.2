<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_overlay_sheet"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Drag Handle -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:background="#2D3748" />

    <!-- Header -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-bold"
                android:text="Memory Vault"
                android:textColor="#FFFFFF"
                android:textSize="20sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="Facts IGIRS AI remembers about you"
                android:textColor="#7E8B9F"
                android:textSize="13sp" />
        </LinearLayout>

        <Button
            android:id="@+id/btnClearAllMemories"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@drawable/bg_model_pill"
            android:paddingHorizontal="12dp"
            android:text="Clear All"
            android:textAllCaps="false"
            android:textColor="#FF4D4D"
            android:textSize="12sp" />
    </RelativeLayout>

    <!-- Empty State -->
    <TextView
        android:id="@+id/tvEmptyMemories"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="32dp"
        android:gravity="center"
        android:text="No memories stored yet.\nSay 'Remember that...' in chat or voice to save facts."
        android:textColor="#4E5B70"
        android:textSize="14sp"
        android:visibility="gone" />

    <!-- Memories List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvMemories"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:maxHeight="400dp"
        android:nestedScrollingEnabled="true" />

</LinearLayout>
